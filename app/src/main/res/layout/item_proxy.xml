<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardProxy"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="?attr/colorSurface">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- IP адрес -->
        <TextView
            android:id="@+id/tvHost"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="185.217.123.45:443"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/tvPing"/>

        <!-- Пинг -->
        <TextView
            android:id="@+id/tvPing"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="45 ms"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="@id/tvHost"
            app:layout_constraintBottom_toBottomOf="@id/tvHost"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Кнопка Копировать (с иконкой и поднятым текстом) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCopy"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="Копировать"
            android:textSize="13sp"
            android:paddingHorizontal="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="0dp"
            app:icon="@android:drawable/ic_menu_save"
            app:iconGravity="textStart"
            app:iconPadding="4dp"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_marginTop="12dp"
            android:layout_marginStart="0dp"
            app:layout_constraintTop_toBottomOf="@id/tvHost"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Кнопка Telegram  -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnConnect"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="Добавить в Telegram"
            android:textSize="13sp"
            android:paddingHorizontal="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="0dp"
            app:iconGravity="textStart"
            app:iconPadding="4dp"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="0dp"
            app:layout_constraintTop_toBottomOf="@id/tvHost"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>